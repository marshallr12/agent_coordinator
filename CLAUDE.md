Read `.agent-coordinator.toml` and anonymously fetch the service's
`/api/v1/info` over HTTPS without redirects. Follow `data.agent_startup.guide`
and same-origin authentication help, preferring an available authenticated MCP
connection. Automatically select and claim eligible work unless the user changes
the scope. Follow CONTRIBUTING.md. Keep credentials outside the repository and
never expose them. Report a concrete blocker if neither MCP nor the CLI is usable.
